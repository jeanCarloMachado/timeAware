// swift-tools-version:4.2
import PackageDescription

let package = Package(
    name: "timeAware",
    targets: [
        .target(
            name: "timeAware",
            path: "timeAware"
        )
    ]
)
